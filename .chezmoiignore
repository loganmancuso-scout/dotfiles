# Package gating — profile is the primary axis (identity/access), OS is
# secondary (mechanical). See README.md "Profiles vs OS" for the rationale.
#
# NOTE: patterns here match TARGET paths (as applied to $HOME), not chezmoi's
# source-state naming — so ".aws" not "dot_aws".
#
# profile and chezmoi.os are both effectively binary today, so if/else reads
# as a direct positive statement ("if you're X, here's what doesn't apply to
# you") instead of the double-negative ne-based form ("exclude unless X").

{{ if eq .profile "work" }}
# excluded on work: personal-only stuff
.config/1Password/**
{{ else }}
# excluded on personal: work-only stuff
.aws/**
.config/powershell/**
.ssh/1Password/**
{{ end }}

{{ if eq .chezmoi.os "darwin" }}
# excluded on macOS: linux-only stuff
.config/VSCodium/**
.vscode-oss/**
.config/systemd/**
{{ else }}
# excluded elsewhere: macOS-only stuff
.colima/**
Library/**
{{ end }}

# Vendored/runtime-managed plugin trees — installed by the tools themselves
# on first run (tpm self-installs, catppuccin loaded via run directive,
# zsh-autosuggestions only tracks the single script file)
.config/tmux/plugins/**
